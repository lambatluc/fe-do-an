export interface ILiveStream {
    id: string
    createdAt: string
    streamerName: string
    title: string
    titleImage: string
    categories: string
    rating: number
  }
export interface ICategory {
  id: string
  title: string
}